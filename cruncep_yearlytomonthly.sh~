#!bin/bash

#This file calculates the 30-year running climatology for the PalEON domain from the output met drivers
#Jaclyn Hatala Matthes, 9/26/13

filedir='/projectnb/paleon/met_drivers/'
years=`ls *.h5 | cut -c 6-9 | uniq`
months=`ls *.h5 | cut -c 10-12 | uniq`

for year in ${years} #loop over years
do
    for month in ${months} #loop over months
    do 
	


    done
done
